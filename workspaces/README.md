ここにソースファイルを配置する。

# 環境

VSCodeで開発環境コンテナを使う前提。
コンテナで開くと、webコンテナにアタッチする。

## devcontainer.json

ワークスペースの設定、マウントディレクトリを記述。
ワークスペースのマウントもしており docker-compose.yml には記述していない。
angularをインストールしている。バージョンを指定したい場合記述を変更してコンテナをリビルドする。
ワークスペース内で `ng new my-app` を実行してアプリケーションを作成する。
